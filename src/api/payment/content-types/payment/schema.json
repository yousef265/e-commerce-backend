{
  "kind": "collectionType",
  "collectionName": "payments",
  "info": {
    "singularName": "payment",
    "pluralName": "payments",
    "displayName": "payment",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "payment_status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "paid",
        "failed"
      ],
      "default": "pending",
      "required": true
    },
    "amount_paid": {
      "type": "decimal",
      "required": true
    },
    "payment_method": {
      "type": "enumeration",
      "enum": [
        "credit_card",
        " paypal",
        "cash"
      ],
      "required": true
    }
  }
}
